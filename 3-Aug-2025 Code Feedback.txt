Feedback for code uploaded until 3-Aug-2025 
1. General Comments 
a. As Python version 3.11, specified in the project, is getting into security maintenance mode, can we upgrade to the latest version of Python b. Please use the fast api developers recommend full stack application  template - https://github.com/fastapi/full-stack-fastapi-template 
c. What is your recommendation for using the below project scaffolding for  each module    
d. Will you be implementing migration and seeding for each module i. If data is not present, please seed as needed at app startup 
e. Rectify all pylance errors 
f. Cancellation token equivalent in fast api 
g. Testing?  
h. Add luanc.json file to start the application in debug mode 
2. eshop/config/db.py 
settings = Settings() 
engine = create_async_engine(settings.database_url, echo=True) AsyncSessionLocal = async_sessionmaker(engine, class_=AsyncSession,  expire_on_commit=False)
async def get_db() -> AsyncGenerator[AsyncSession, None]: 
"""Dependency to get database session.""" 
async with AsyncSessionLocal() as session: 
try: 
yield session 
Finally: 
await session.close()  
a. Please start the data database session using async_session maker b. Please add type hints wherever possible 
3. eshop\core 
a. Please add the contracts folder and content 
b. mediator pattern implementation?  
c. Unit testing for the core module?  
4. eshop\core\domain\entity.py line 15 – add timestamp with timezone 5. eshop\core\health\health_service.py – 
a. Line 97 remove deprecated function (utcnow) – use timestamp with  timezone 
b. Line 110: Remove the deprecated function Close() 
6. eshop\core\logging\logger.py 
a. Are we implementing auto logging? i.e, log captured at different levels  without explicitly calling log statements. Are we capturing Uvicron Server  Logs? Do we need to put server logs in a different file? 
b. Are all log entries part of an async operation 
7. eshop\core\mapping\mapper.py 
a. We have three different models – DTO model, domain model, and ORM model. I saw the DTO to entity/domain model mapping and vice versa, but  I did not see the ORM model mapping 
8. eshop\core\messaging\integration_event.py 
a. Autoset topic and event name whereever possible  
b. Are we explicitly setting the event name 
i. ICan we capture the event name from the class triggering the event ii. How does this apply to RabbitMQ routing key 
9. eshop\core\repr\base.py 
a. just for reference – request -> command / query -> result -> reponse  10. eshop\core\auth\keycloak.py 
a. key cloak wrapper - fastapi-keycloak (preferred), python-keycloak, and  authlib 
b. 
11. eshop\main.py 
a. graceful startup and stop of a fast api application?